package com.fingenie.ui.groups

import androidx.lifecycle.ViewModel

class AddGroupViewModel : ViewModel() {
    // This ViewModel is currently empty as the group creation logic
    // is handled directly in the Fragment. We can add more functionality
    // here if needed in the future, such as:
    // - Member search and selection
    // - Group template management
    // - Group settings
} 